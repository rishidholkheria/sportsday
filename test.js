// let arr = [1,2,3,4,5]

// arr.forEach((item, index) => {
//   setTimeout(() => {
//     console.log(item);
//   }, index * 2000); // 2000ms = 2s
// }); 

// for (F

var participants = {
        red : 0,
        blue : 0,
        green : 0,
        black : 0,
}

participants.map((item, index) => {
    console.log(`${item} : ${index}`)   
})
